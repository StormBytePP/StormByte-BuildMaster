set(_bm_edge_list "${BM_TEST_EXPECTED_DIR}/dependant_edges.txt")
if(NOT EXISTS "${_bm_edge_list}")
	message(FATAL_ERROR "Missing ${_bm_edge_list}")
endif()

set(_bm_fail FALSE)
set(_bm_edge_count 0)

file(STRINGS "${_bm_edge_list}" _bm_lines)
foreach(_line IN LISTS _bm_lines)
	string(STRIP "${_line}" _line)
	if(_line STREQUAL "" OR _line MATCHES "^#")
		continue()
	endif()

	if(NOT _line MATCHES "->")
		message(WARNING "Malformed edge line: ${_line}")
		continue()
	endif()

	string(REPLACE "->" ";" _parts "${_line}")
	list(GET _parts 0 _from)
	list(GET _parts 1 _to)
	string(STRIP "${_from}" _from)
	string(STRIP "${_to}" _to)

	if(NOT TARGET ${_from})
		message(SEND_ERROR "Edge: dependent target '${_from}' does not exist")
		set(_bm_fail TRUE)
		continue()
	endif()
	if(NOT TARGET ${_to})
		message(SEND_ERROR "Edge: prerequisite target '${_to}' does not exist")
		set(_bm_fail TRUE)
		continue()
	endif()

	get_target_property(_deps ${_from} MANUALLY_ADDED_DEPENDENCIES)
	if(_deps STREQUAL "_deps-NOTFOUND" OR NOT _deps)
		set(_deps "")
	endif()

	list(FIND _deps "${_to}" _idx)
	if(_idx EQUAL -1)
		message(SEND_ERROR "Edge missing: ${_from} -> ${_to} (have: ${_deps})")
		set(_bm_fail TRUE)
	else()
		math(EXPR _bm_edge_count "${_bm_edge_count} + 1")
	endif()
endforeach()

if(_bm_fail)
	message(FATAL_ERROR "BuildMaster dependant edge checks failed")
endif()

message(STATUS "BuildMaster dependant edges: OK (${_bm_edge_count} checked)")