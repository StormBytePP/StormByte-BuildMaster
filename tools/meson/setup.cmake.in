# Generated by BuildMaster – component configure stage (Meson)

set(_BM_STAGE_CONFIGURE "@_MESON_STAGE_CONFIGURE@")
set(_BM_STAGE_BUILD "@_MESON_STAGE_BUILD@")

if(NOT TARGET ${_BM_STAGE_CONFIGURE})
	add_custom_target(${_BM_STAGE_CONFIGURE}
		COMMAND ${CMAKE_COMMAND} -E echo "Configuring @_MESON_COMPONENT_TITLE@"
		COMMAND ${CMAKE_COMMAND} -P "@_MESON_SETUP_EXEC_SCRIPT@"
		USES_TERMINAL
		VERBATIM
	)
	if(TARGET buildmaster_build_init)
		add_dependencies(${_BM_STAGE_CONFIGURE} buildmaster_build_init)
	endif()
endif()

if(TARGET ${_BM_STAGE_BUILD})
	add_dependencies(${_BM_STAGE_BUILD} ${_BM_STAGE_CONFIGURE})
endif()
